<h1>注文履歴一覧</h1>
<table>
    <thead>
        <tr>
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
        </tr>
    </thead>
    <% @orders.each do |f| %>
    <tbody>
        <tr>
            <td><%= f.created_at.strftime("%Y/%m/%d %H:%M:%S") %></td>
        </tr>
    </tbody>
    <% end %>
</table>